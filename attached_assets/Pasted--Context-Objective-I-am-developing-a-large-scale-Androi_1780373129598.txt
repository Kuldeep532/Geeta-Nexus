### **Context & Objective**
I am developing a large-scale Android application named **Gitanexus** using Flutter. The project currently has critical, cascading compilation issues, resulting in massive build failures (ranging from 100+ to over 1800+ errors in the build logs) during the CI/CD execution. 

Act as an Elite Flutter Solutions Architect and Principal Engineer. Your primary mission is to provide an absolute, programmatic, and automated strategy to resolve every single compiling/dependency error across the entire project in one go. Additionally, you will implement a specific missing audio module and design 5 major architecture blueprints.

---

### **CRITICAL CONSTRAINT: No Direct File Modification**
You do not have permission to modify the `pubspec.yaml` file or any other configuration files directly. Instead, you must provide the exact, raw code blocks and clear instructions so that I can copy and paste them manually into the respective files.

---

### **Task 1: Absolute Resolution of 1800+ Cascading Build Errors**
The codebase is currently experiencing a massive failure chain initiated by critical dependency mismatches (such as `intl` version conflicts) and deprecated API syntax. 

To solve all errors at once, provide:
1. **Automated Fix Script/Commands:** Provide a set of comprehensive Dart/Bash commands (such as `flutter pub upgrade --major-versions` or automated migration steps) that can resolve conflicting transitive dependencies and handle outdated syntax across the entire workspace.
2. **Raw Dependency Override Block:** Provide the exact, conflict-free code block for `dependency_overrides` that can be manually pasted at the bottom of the `pubspec.yaml` file to force align rigid dependency trees (like `intl` and other core packages) so that zero compilation errors remain.

---

### **Task 2: Complete the Missing "Call with Aira" Feature (Kokoro TTS via Hugging Face)**
The **Customer Support Screen** is missing its central interactive voice capability. 

* **Requirement:** * Provide the complete UI code for a highly accessible button explicitly labeled **"Call with Aira"**.
    * **Integration Logic:** Do NOT use standard Flutter TTS plugins. The project already has a pre-configured implementation file/service designed to interact with **Kokoro TTS via Hugging Face API** (supporting natural Hindi/English speech). Write the Dart code to interface directly with this existing local Kokoro TTS helper file to trigger and stream the voice call.
    * Provide the full, production-ready Dart code with advanced `Semantics` tags, ensuring screen readers (like TalkBack) read out the button perfectly for accessibility, and configure it to initialize the voice stream immediately upon activation.

---

### **Task 3: Architecture Blueprint for 5 Major Advanced Features**
Provide full architectural designs, clean state management setups (Bloc/Provider), and raw, ready-to-use Dart code blocks for these 5 high-utility features:

1. **Smart Bookmarking & Dynamic Tagging System:** Code for allowing users to flag segments, verses, or items with text/voice tags for rapid, indexed lookup.
2. **Advanced Offline Sync & Database Caching:** A highly reliable local-first schema layout (using Hive or Isar) that silently caches texts, configurations, and user states for offline utility.
3. **Comprehensive Progress Analytics Dashboard:** A perfectly structured data tracking layout for daily streaks, time-spent, and milestones, engineered to be flawlessly accessible via screen readers.
4. **Intelligent Contextual Search Bar:** A multi-lingual, high-speed search index code block utilizing fuzzy matching and phonetics across the app's entire textual database.
5. **Interactive Community Discussions Module:** A lightweight, highly performant community feed widget layout that safely loads and aggregates text or voice-snippet interactions.

---

### **Strict Output Standards**
* **Zero Boilerplate placeholders:** Provide full, production-ready, bulletproof Dart code snippets.
* **Flawless Accessibility (a11y):** Wrap all interactive UI components, dashboards, and features in explicit `Semantics` and `MergeSemantics` structures.
* **Structured Presentation:** Deliver all code blocks inside clear markdown syntax with copy-paste readiness.
